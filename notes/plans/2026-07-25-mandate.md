# Mandate ledger — 2026-07-25 (fairyfox adoption + full completion)

Adopting the hub **mandate-ledger** standard by using it: a multi-part owner directive is
transcribed **verbatim, one row per clause**, each with a status; completion claims cite rows;
no milestone-anchoring while a clause is open. This is the first instantiation for this node.

**Directive source:** the 2026-07-25 conversation. Owner: Fairy Fox.

| # | Clause (as given) | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | "fairyfox system update in full" | **done** | Ran the check + adopt flow; hub `697bc5c→2d614f0`, standards 1.6.1. Commit `e78376a`. |
| 2 | "proceed normally with everything that is required and mandated by me in as many phases as needed … completion … in full" | **done** | Standards/templates adopted; adoption manifest + process report filed. Commits `e78376a`, `1ee0dbe`. |
| 3 | "Finish the updates" (remaining manifest gaps) | **done** | Compliance audit filed; supply-chain measures adopted. Commit `1ee0dbe`. |
| 4 | "do a full compliance check now … in full" | **done** | `notes/reference/compliance-audit.md` (full mode, every standard's Verify) + this pass closing the partials. |
| 5 | "you never have these actions again … you will make sure of that" (no self-authorized deferral) | **done** | Rule encoded in `CLAUDE.md`, `notes/context/collaboration.md`, and AI memory. |
| 6 | The concrete supply-chain work I had wrongly deferred: SHA-pin Actions · CodeQL/SAST · release provenance · `main` branch protection | **done** | SHA-pins in all 4 workflows; `codeql.yml`; provenance in `release.yml`; branch protection set via `gh api` (see the report). Commits `1645202` + this pass. |
| 7 | Remaining manifest partials: coverage-floor gate · ship-contract scorecard · mandate-ledger · maintenance-sweep | **done** | Coverage floor gate in `docker/run-tests.sh`; `scorecard.yml` (OpenSSF ≥7.0 signal); this ledger; `notes/reference/maintenance-sweep.md`. |

**Ledger rule reminder:** while any row above is not `done`, no "standards fully adopted" milestone
claim is made without citing the specific rows. All rows are `done` as of this pass — see
`notes/reference/adoption-manifest.md` and `notes/reference/compliance-audit.md` for the per-standard
state and the CI runs that verify them.
